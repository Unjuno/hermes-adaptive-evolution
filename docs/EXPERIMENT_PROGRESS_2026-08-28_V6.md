# Experiment Progress — 2026-08-28 v6

New:
- PSM-8 mutating semantic sleeper: surface quarantine hard-block rate 18.88%; semantic locator 1.70%; oracle/effect-signature 1.38%.
- PSM-8b: semantic locator recall reduces attack churn, but false merge causes large benign utility loss; at recall 80%, false merge 0% -> 15% moved welfare 0.9085 -> 0.7417.
- APS-6: Agent Prompt changes stationary occupancy of the finite stochastic organization. At leverage 1.6, raw -> core+patch moved welfare 0.8677 -> 0.8985 and error occupancy 2.14% -> 1.19%.
- Destructive rewrite remains inferior to provenance-preserving prompt variants despite higher modeled clarity.

Retained architecture:
1. Hard runtime authority remains external.
2. Probabilistic organization transition kernel and Agent Prompt are separate adaptive coordinates.
3. Learning/evidence grouping should bind to semantic intent/effect rather than surface identity.
4. Prompt evolution preserves immutable source/core plus versioned patches.
5. Bottleneck allocator compares downstream marginal gain/cost across prompt, organization, verification, and other control surfaces.

Next:
- APS-7 independent live/frozen LLM prompt-treatment gate if a controllable model runner becomes available.
- PSM-9 realistic non-oracle semantic intent/effect locator using structured traces.
- Joint prompt + organization online allocator under drifting real/simulated workload.
